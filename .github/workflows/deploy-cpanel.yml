name: 🚀 Deploy to cPanel via FTP

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    name: Upload to cPanel FTP
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: 🚚 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.tripstins.com #${{ secrets.FTP_HOST }}
          username: sadimahmud225@pcist.tripstins.com #${{ secrets.FTP_USERNAME }}
          password: sadimahmud338@gmail # ${{ secrets.FTP_PASSWORD }}
          server-dir: /pcist.tripstins.com/sadimahmud225/
          port: 21
          protocol: ftps
